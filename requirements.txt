fastapi>=0.115
uvicorn[standard]>=0.32
pydantic-settings>=2.6
httpx>=0.27
apscheduler>=3.10
aiosqlite>=0.20
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0
python-dotenv>=1.0

# 真实采集（browser provider）。用本机 Chrome/Edge，无需 playwright install chromium
playwright>=1.45

# 开发与测试
pytest>=8.3
pytest-asyncio>=0.24

# 文档构建（tools/build_docs_html.py：把 docs/*.md 合并成单文件 HTML）
markdown>=3.6
